# Do all quantities have the same dimension?

Do all quantities have the same dimension?

## Usage

``` r
.isSameDimension(quantities)
```

## Arguments

- quantities:

  Quantities which dimensions are compared

## Value

TRUE if the dimension of all quantities are equal, otherwise FALSE
