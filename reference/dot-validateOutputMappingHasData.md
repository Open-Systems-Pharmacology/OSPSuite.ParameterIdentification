# Validate observed data availability in `PIOutputMapping`

Ensures each `PIOutputMapping` object has observed datasets. Throws an
error if no observed data is found.

## Usage

``` r
.validateOutputMappingHasData(object)
```

## Arguments

- object:

  A `PIOutputMapping` object or a list of `PIOutputMapping` objects.
